import { ModuleRegistry, Logger } from '@n8n/backend-common';
import { type AuthenticatedRequest } from '@n8n/db';
import { ChatHubSettingsService } from './chat-hub.settings.service';
import { UpdateChatSettingsDto } from './dto/update-chat-settings.dto';
export declare class ChatHubSettingsController {
    private readonly settings;
    private readonly logger;
    private readonly moduleRegistry;
    constructor(settings: ChatHubSettingsService, logger: Logger, moduleRegistry: ModuleRegistry);
    getSettings(): Promise<{
        chatAccessEnabled: boolean;
    }>;
    updateSettings(_req: AuthenticatedRequest, _res: Response, dto: UpdateChatSettingsDto): Promise<{
        chatAccessEnabled: boolean;
    }>;
}
